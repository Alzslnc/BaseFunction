<Window x:Class="Progress.InputBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Progress"      
        mc:Ignorable="d"
        MouseLeftButtonDown="Move"
        WindowStartupLocation="CenterOwner"
        Title="InputBox" 
        Width="250" 
        SizeToContent="Height" 
        WindowStyle="None" 
        ResizeMode="NoResize"
        FocusManager.FocusedElement="{Binding ElementName=TextBox}">
    <!-- ИСПРАВЛЕНО: Фокус сразу в TextBox -->
    <Grid>
        <Border Margin="0" BorderThickness="1" BorderBrush="Black">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <TextBlock TextWrapping="Wrap" VerticalAlignment="Center" x:Name="TextBlock" Margin="5,5,5,5" Grid.ColumnSpan="2" TextAlignment="Center" HorizontalAlignment="Center" />
                <TextBox x:Name="TextBox" Grid.Row="1" Grid.ColumnSpan="2" Margin="5,5,5,5"/>

                <!-- ИСПРАВЛЕНО: Добавлен IsDefault="True" для автоматической обработки клавиши Enter -->
                <Button Grid.Row="2" Content="Принять" Margin="5" Click="Button_Click" IsDefault="True"/>

                <!-- ИСПРАВЛЕНО: Добавлен IsCancel="True" для автоматической обработки клавиши Escape -->
                <Button Grid.Row="2" Grid.Column="1" Content="Отменить" Margin="5" Click="Button_Click_1" IsCancel="True"/>
            </Grid>
        </Border>
    </Grid>
</Window>